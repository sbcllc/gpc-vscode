# GCP Project Configuration
# Required: Your GCP project ID
GCP_PROJECT_ID="your-project-id"

# Required: Comma-separated list of email addresses allowed to access via IAP
# Example: "user1@gmail.com,user2@example.com"
IAP_ALLOWED_EMAILS="your-email@gmail.com"

# Optional: GCP region for resources (default: us-central1)
GCP_REGION="us-central1"

# Optional: GCP zone for VM (default: us-central1-a)
GCP_ZONE="us-central1-a"

# Optional: VM machine type (default: e2-medium)
VM_MACHINE_TYPE="e2-medium"

# Optional: VM name (default: code-server-vm)
VM_NAME="code-server-vm"

# Optional: Boot disk size in GB (default: 50)
BOOT_DISK_SIZE="50"

# Optional: Boot disk type (default: pd-balanced)
BOOT_DISK_TYPE="pd-balanced"

# Optional: code-server version to install (default: latest)
CODE_SERVER_VERSION=""

# Optional: Enable HTTPS load balancer with custom domain
# Set to "true" to enable (default: false)
ENABLE_HTTPS_LB="false"

# Required if ENABLE_HTTPS_LB is true: Custom domain for HTTPS access
# Example: "code.yourdomain.com"
CUSTOM_DOMAIN=""

# Optional: Service account email for the VM
# If not set, default compute service account is used
SERVICE_ACCOUNT=""

# Optional: Network tags for the VM (comma-separated)
VM_NETWORK_TAGS="iap-tunnel,code-server"

# Optional: Labels for resources (comma-separated key=value pairs)
RESOURCE_LABELS="environment=development,app=code-server"
